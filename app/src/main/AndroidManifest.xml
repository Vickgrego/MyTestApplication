<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="solutions.boost.tvprogramm">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ensure service runs in different process -->
        <service
            android:name="solutions.boost.internetservice.ServerService"
            android:exported="false"
            android:enabled="true"
            android:process=".serviceprocess">
        </service>

        <activity android:name=".ProgramScheduleActivity">
        </activity>
        <activity android:name=".ChannelsForCategoryActivity">
        </activity>
    </application>

</manifest>